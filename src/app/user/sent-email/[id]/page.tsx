// 'use client'

const SentEmail = async (props: unknown) => {
    // const { params: { id: userId } } = props;

    // TODO: Show more 
    // useEffect(() => {
    //     // get the user id
    // }, [userId])

    return (
        <>
            <h2>Please check your email for a login link</h2>
        </>
    )
}

export default SentEmail;
